--PFFFT LOL
--this is hilarious.

--Todo
--Completed.
--Maybe find a way to run 'select' in shell with select.lua not in root?
--These convience functions are nice, but they are filling root fast.

-- -anyway, TWO lines of code that can, even readably, be one. watch.
args = {...}

turtle.select(tonumber(args[1]))

--turtle.select(tonumber(...))
--well maybe not understood but..